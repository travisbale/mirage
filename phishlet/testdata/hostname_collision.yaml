name: collider
author: "@testauthor"
version: "1"

proxy_hosts:
  - phish_sub: login
    orig_sub:  login
    domain:    example.com
    is_landing: true

auth_tokens:
  - domain: ".example.com"
    keys:
      - name: session
        required: true
